Steps:
	-- install vs2019
	-- open and build WebGoat.net solution
	-- install mysql-installer-community-5.7.34.0
	-- create a DB in mysql from mySQL client tool
	-- launch debug with IIS express from vs2019
	-- proceed to 'recreate DB' from the WebGoat portal
	   'Data Provider', choose MySql.
	   'Client Executable' and 'Data File Path', leave them empty.
	   'Database', fill in the name of DB you just created.


